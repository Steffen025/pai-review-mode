{
  "name": "pai-review-mode",
  "version": "0.1.0",
  "description": "Platform-Agnostic Review Mode for PAI agents — True Dual-Context isolation for processing untrusted code contributions",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:adversarial": "vitest run tests/adversarial/",
    "test:bench": "vitest run tests/benchmarks/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "author": "Steffen025",
  "repository": {
    "type": "git",
    "url": "https://github.com/Steffen025/pai-review-mode.git"
  },
  "keywords": [
    "pai",
    "review-mode",
    "security",
    "hmac",
    "quarantine",
    "content-filter",
    "dual-context",
    "agent-security"
  ]
}
